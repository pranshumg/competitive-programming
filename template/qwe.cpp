#include <bits/stdc++.h>

using namespace std;

#define eb emplace_back
#define ff first
#define ss second
#define all(x) (x).begin(), (x).end()

inline int ri() {
  int x;
  cin >> x;
  return x;
}

void run_case() {
  ${0}
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int tests;
  cin >> tests;
  while (tests--) {
    run_case();
  }
  return 0;
}