{
  "cmd": [
    "cmd", "/c",
    "g++ -std=c++20 \"${file}\" -o \"${file_base_name}.exe\" && \"${file_base_name}.exe\" < input.txt > output.txt"
  ],
  "file_regex": "^(.*?):([0-9]+):([0-9]+):",
  "selector": "source.c++"
}
