/**
 *   author: pranshumg
**/
#include <bits/stdc++.h> 

using namespace std;

int main() {    
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t, a, b;
    cin >> t;
    char c;
    while (t--) {
        cin >> a >> c >> b;
        cout << a + b << '\n';
    } 
    return 0;
}