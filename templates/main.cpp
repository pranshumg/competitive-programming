#include <bits/stdc++.h>

using namespace std;

#ifdef PRANSHU_DEBUG
#include "debug.h"
#else
#define dbg(...) 24
#endif

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int tc;
  cin >> tc;
  while (tc--) [&]() -> void {
    $0
  }();
  return 0;
}