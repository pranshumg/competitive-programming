{
  "shell_cmd": "g++ -std=gnu++23 '${file}' -o '${file_path}/${file_base_name}' && '${file_path}/${file_base_name}' < input.txt > output.txt",
  "working_dir": "${file_path}",
  "file_regex": "^(.*?):([0-9]+):([0-9]+):",
  "selector": "source.c++"
}