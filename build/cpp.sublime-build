{
  "shell_cmd": "g++ -std=c++20 '${file}' -o '${file_path}/${file_base_name}' 2> error.txt && '${file_path}/${file_base_name}' < input.txt > output.txt 2>> error.txt",
  "working_dir": "${file_path}",
  "file_regex": "^(.*?):([0-9]+):([0-9]+):",
  "selector": "source.c++"
}
